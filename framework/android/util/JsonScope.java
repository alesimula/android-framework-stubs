package android.util;

enum JsonScope {
    CLOSED,
    DANGLING_NAME,
    EMPTY_ARRAY,
    EMPTY_DOCUMENT,
    EMPTY_OBJECT,
    NONEMPTY_ARRAY,
    NONEMPTY_DOCUMENT,
    NONEMPTY_OBJECT;
    private static final android.util.JsonScope[] $VALUES = null;
    private JsonScope() {}
}
